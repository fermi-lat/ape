get_filename_component(ape_CMAKE_DIR "${CMAKE_CURRENT_LIST_FILE}" PATH)
include(CMakeFindDependencyMacro)

if(NOT TARGET ST::ape)
    include("${ape_CMAKE_DIR}/apeTargets.cmake")
endif()

set(ape_LIBRARIES ST::ape)
